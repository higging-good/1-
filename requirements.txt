easyocr==1.7.2
numpy>=1.24,<2
opencv-python==4.8.1.78
ultralytics>=8.3,<9

# ROS 2 packages (rclpy, cv_bridge, sensor_msgs, geometry_msgs, std_msgs)
# are installed through the system's ROS 2 distribution, not pip.
